"""
strands/models/__init__.py — stub
----------------------------------
BedrockModel is not used in the boto3.converse() implementation.
Provided as a stub so any residual import doesn't fail.
"""


class BedrockModel:
    """Stub — not used. Model config is passed directly to boto3.converse()."""
    def __init__(self, *args, **kwargs):
        pass
